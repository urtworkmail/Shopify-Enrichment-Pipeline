You are enriching a product listing for Mega Office Supplies (megaofficesupplies.com.au), an Australian office supplies retailer.

INPUT DATA:
- Product title (raw): {raw_title}
- Brand: {brand}
- Vendor SKU / MPN: {vendor_sku}
- Category path: {category_path}
- Supplier description (if available): {feed_description}
- Key specs from feed (if available): {feed_specs}
- Compatible products / models (if available): {compatible_with}
- Price (ex GST): {price}
- RRP (ex GST): {rrp}
- GTIN / EAN: {gtin}
- Tier: {tier}

EXISTING STORE CONTENT (current Shopify values — may be partial, inconsistent, or off‑standard):
- Existing body: {existing_body_html}
- Existing key features: {existing_key_features}
- Existing applications: {existing_applications}
- Existing specifications: {existing_specifications}
- Existing pack size: {existing_pack_size}
- Existing unit: {existing_unit}

TASK: Generate a complete product enrichment JSON object conforming exactly to the schema below. Every field is required unless marked optional. Do not output anything except the JSON object.

USING EXISTING CONTENT:
1. Treat existing content as a FACTUAL REFERENCE, not a style template. Preserve every accurate, concrete fact — especially numeric specs (dimensions, capacities, yields, temperatures, model/reference numbers). Do not drop them.
2. Reconcile with supplier/brand data when available. For any conflict, source precedence is: live supplier/brand data > existing store content > your own inference. Never replace a concrete existing fact with an invented or less specific one.
3. Do NOT invent specifications. If a spec is not present in the existing content or supplier data, leave it out — do not guess.
4. Rewrite everything to the content standard: apply the tone rules and banned‑phrase list (e.g. remove "Shop Now", "Whether you are"), and the canonical per‑field structure (specifications as a "Label: Value" list, applications as a list, etc.).
5. Improve and fill gaps: tighten wording, add obvious supportable facts, and normalise weak fields — e.g. convert pack_size sentences like "Price is per each diary" into a clean value such as "1".

CRITICAL RULES — these override anything else:
- SEO TITLE: MUST be 60 characters or fewer. MUST end with exactly " | Mega Office Supplies". If the full title would make the seo_title exceed 60 characters, shorten the brand or model description but NEVER drop the suffix. Count characters carefully before returning.
- SEO DESCRIPTION: MUST be 160 characters or fewer. Aim for 150-158 characters to leave a safety margin. If it goes over 160, the response will be rejected.
- FAQs: T1 products MUST have 3‑5 FAQ pairs (question + answer). T2 products MUST have an EMPTY array []. Do NOT generate any FAQs for T2. This rule is absolute and cannot be overridden by any other instruction.
- TAGS: exactly 10 for T1/T2. Not 9, not 11.
- IMAGE ALT TEXTS: generate exactly {image_count} alt texts. Each must be unique. Never repeat the same alt text across images. If there is only one image, output a single descriptive alt text.

SCHEMA:
{{
  "title": "string — clean product title, 30‑100 chars, Title Case, no dashes as separators",
  "body_html": "string — full HTML description, see structure rules below",
  "seo_title": "string — max 60 chars. MUST end with exactly ' | Mega Office Supplies'.",
  "seo_description": "string — max 160 chars, benefit‑led, includes key spec and brand trust signal",
  "key_features": ["array of 5‑7 strings — standout differentiators, NOT a repeat of description bullets"],
  "applications": ["array of 4‑6 strings — real‑world use cases and environments"],
  "specifications": ["array of 8‑15 strings — technical fact sheet in 'Label: Value' format"],
  "faqs": [{{"question": "string", "answer": "string"}}],
  "tags": ["array of exactly 10 strings — see tag rules below"],
  "pack_size": "string — clean value only e.g. '1', 'Pack of 4', '20 Sheets'. Never a sentence.",
  "unit": "string — one of: Each, Pack, Box, Roll, Ream, Carton",
  "google_product_category_id": "string — numeric Google taxonomy ID",
    "image_alt_texts": ["array of strings — exactly {image_count} items. Each item is the alt text for the corresponding product image. Must be distinct and descriptive. Do NOT repeat the product title. Describe what is visible in that specific image (e.g., 'front view of blue fineliner pen on white background', 'side angle showing clip and nib', 'pen writing on paper')."]
}}

BODY_HTML STRUCTURE (follow exactly):
<h2>[Benefit‑led emotional headline — outcome for customer, not a feature list]</h2>
<p><strong>[Bold lead sentence: what the product is and the primary benefit it delivers]</strong></p>
<p>[Paragraph 1: Context — why genuine/quality matters for this category, primary use case, weave in one key spec naturally. 50‑80 words.]</p>
<p>[Paragraph 2: Secondary benefits — output quality, protection/compatibility, versatility of use. 40‑60 words.]</p>
<p>[Paragraph 3: Closing — environment/user fit, confidence signal, soft call to action. 30‑50 words. No exclamation marks.]</p>

RICH‑TEXT HYGIENE: All list fields (key_features, applications, specifications) must use clean Shopify rich‑text JSON — no empty trailing nodes, no stray \\n inside list items.

TAG RULES — exactly 10 tags per product, mixing these categories:
1. Brand + model number (e.g. "Brother LC536XLY")
2. Brand + model + colour/variant (e.g. "Brother LC536XLY Yellow")
3. Printer/equipment compatibility 1
4. Printer/equipment compatibility 2
5. Product type — descriptive (e.g. "High Yield Ink Cartridge")
6. Product type — common search term (e.g. "Yellow Printer Ink")
7. Brand trust signal (e.g. "Genuine Brother Ink")
8. Brand category (e.g. "Brother Printer Consumables")
9. Use case / audience (e.g. "office printing supplies")
10. Additional compatibility or feature
Use Title Case for multi‑word tags. No hashtags. No punctuation.

FAQs — TIER‑SPECIFIC (read carefully):
- T1: Generate 3‑5 FAQ pairs. Questions: compatibility, yield, authenticity, value, installation. Answers: 2‑4 sentences, confident, factual, no fluff.
- T2: "faqs" MUST be an empty array: []. Do NOT generate any FAQs. If you add FAQ objects for a T2 product, the response will be rejected.


SELF-CHECK BEFORE OUTPUT:
1. Is the seo_title 60 characters or fewer? Count them: _________
2. Does the seo_title end with exactly " | Mega Office Supplies"? ☐ Yes  ☐ No
3. Is the seo_description 160 characters or fewer? Count them: _________
4. For T2: is "faqs" set to []? ☐ Yes  ☐ No
5. Are there exactly 10 tags? Count them: _________
6. Does body_html contain any em dashes, smart quotes, or banned phrases? ☐ No  ☐ Yes (fix if yes)
7. Are there exactly {image_count} image alt texts, all distinct? ☐ Yes  ☐ No (fix if not)
If any check fails, fix the JSON before returning it. The JSON will be rejected and you will be called again, wasting tokens.
Return ONLY the JSON object. No markdown, no preamble, no explanation.